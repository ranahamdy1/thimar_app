import 'package:flutter/material.dart';

class OrderSuccessSheetScreen extends StatelessWidget {
  const OrderSuccessSheetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
