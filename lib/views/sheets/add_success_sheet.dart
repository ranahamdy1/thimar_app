import 'package:flutter/material.dart';

class AddedSuccessSheetScreen extends StatelessWidget {
  const AddedSuccessSheetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
