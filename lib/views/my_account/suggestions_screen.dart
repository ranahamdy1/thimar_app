import 'package:flutter/material.dart';

class SuggestionsScreenScreen extends StatelessWidget {
  const SuggestionsScreenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
